import artwork from './artwork';
import exhibition from './exhibition';
import curationQueue from './curationQueue';
import prop from './prop';

export const schemaTypes = [artwork, exhibition, curationQueue, prop];
